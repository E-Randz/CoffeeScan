<% include partials/header %>

<a href="/coffeeshops/new">Add New Coffeeshop</a>
<h1>This is the coffeeshops page</h1>

<% coffeeshops.forEach(function(c){ %>
    <div>
        <h4><%= c.name %></h4>
        <img src="<%= c.image %>">
    </div>
<% }); %>

<% include partials/footer %>
